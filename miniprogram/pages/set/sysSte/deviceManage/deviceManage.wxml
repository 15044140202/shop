<view class="page-header" style="height: 8vh;">
  <view class="header-title">物联网设备管理</view>
  <view class="header-intro">物联网设备设置/测试</view>
</view>
<view class="menu-container">
  <view class="menu-card" hover-class="card-hover" wx:for="{{device}}" wx:key="index" bind:tap="goto" mark:item="{{index}}" wx:if="{{index !== 'shopLightSet' && index !== 'cupboardUseRule' && index !== 'shopId' && index !== '_id' && index !== 'sportShowPrice'}}">
    <van-icon class="card-icon" size="100rpx" name="{{index === 'lightCtrl' ? '../../../../icon/吊灯.png' : index === 'camera' ? '../../../../icon/录像机.png' :index === 'announcer' ?'../../../../icon/喇叭.png' : index === 'printer' ? '../../../../icon/打印机.png' :index === 'doorLock' ? '../../../../icon/智能门锁.png':index === 'cupboard' ? '../../../../icon/杆柜.png': index === 'shopSmartDisplay' ? 'cashier-o':''}}" color="#999"></van-icon>
    <view class="card-title">{{index === 'lightCtrl' ? '灯控器' : index === 'camera' ? '精彩秀摄像机' :index === 'announcer' ?'播报器' : index === 'printer' ? 'pos打印机' : index === 'doorLock' ? '智能门禁': index === 'cupboard' ? '智能杆柜' : index === 'shopSmartDisplay' ? '智大慧屏' : ''}}</view>
  </view>
</view>